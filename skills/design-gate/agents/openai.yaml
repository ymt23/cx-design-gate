interface:
  display_name: "CX Design Gate"
  short_description: "Gate AI UI work before implementation passes."
  default_prompt: "Use $design-gate to review this screen from a Project Context Packet and screenshot before implementation is accepted."
policy:
  allow_implicit_invocation: true
